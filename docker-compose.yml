services:
  db:
    image: postgres:16
    container_name: planejadin-db
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: surfamily
      POSTGRES_DB: planejadin
    ports:
      - "${DB_PORT:-5433}:5432"
    volumes:
      - dbdata:/var/lib/postgresql/data
      - ./infra/db/init:/docker-entrypoint-initdb.d:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d planejadin"]
      interval: 5s
      timeout: 3s
      retries: 10
volumes:
  dbdata:
